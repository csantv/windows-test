name: Build
on: push

jobs:
    build:
        runs-on: macos-latest
        steps:
            - name: checkout
              uses: actions/checkout@v2
            - name: checkout
              uses: actions/checkout@v2
              with:
                repository: corpnewt/gibMacOS
            - run: |
                  ls ..
                  echo "==="
                  ls -a
                  echo "==="
                  pwd
